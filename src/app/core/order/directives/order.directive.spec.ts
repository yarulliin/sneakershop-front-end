import { OrderDirective } from './order.directive';

describe('OrderDirective', () => {
  it('should create an instance', () => {
    const directive = new OrderDirective();
    expect(directive).toBeTruthy();
  });
});
